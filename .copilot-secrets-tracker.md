# 🔒 Secrets & Environment Variables Tracker

**⚠️ CRITICAL COPILOT GUARDRAILS ⚠️**

This file tracks which environment variables and secrets exist **WITHOUT exposing their actual values**.

## Rules for AI Assistant:

1. **NEVER** read, display, or modify `.env.local` or any `.env*` files
2. **NEVER** assume env files don't exist - they are configured and working
3. **NEVER** propose "fixing" env configuration unless explicitly asked
4. When Darrell asks about "dev server", "local testing", or "npm run dev":
   - Just run: `npm run dev` (this auto-runs `npm install` first via predev script)
   - Open in Simple Browser if requested
   - Do NOT redesign, reconfigure, or "help" with env setup

---

## Environment Variables Status

### `.env.local` (Local Development)
**Status:** ✅ **CONFIGURED & WORKING**  
**Last Verified:** 2024-02-27  
**DO NOT MODIFY THIS FILE**

Variables configured:
- ✅ `NEXT_PUBLIC_FIREBASE_API_KEY` - Firebase client config
- ✅ `NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN` - Firebase auth
- ✅ `NEXT_PUBLIC_FIREBASE_PROJECT_ID` - Firebase project
- ✅ `NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET` - Firebase storage
- ✅ `NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID` - Firebase messaging
- ✅ `NEXT_PUBLIC_FIREBASE_APP_ID` - Firebase app
- ✅ `GOOGLE_GENAI_API_KEY` - Genkit/Gemini API key (server-side only)
- ✅ `GOOGLE_APPLICATION_CREDENTIALS` - Path to Google Cloud service account JSON
- ✅ `ALLOWED_ORIGINS` - CORS configuration

### Google Cloud Secret Manager (Production)
**Status:** ✅ **CONFIGURED IN CLOUD RUN**

Secrets configured:
- ✅ Firebase credentials (all 6 values)
- ✅ Google AI API key
- ✅ Google Cloud credentials (service account)

---

## Quick Reference for Darrell

### Run Development Server
```bash
npm run dev
# This automatically runs npm install first, then starts dev server
# Opens on http://localhost:3003 (or 3000 if available)
```

### Build for Production
```bash
npm run build
```

### Deploy to Cloud Run
```bash
git push
# Cloud Run auto-deploys from GitHub
```

---

## For IT/DevOps Only

If you need to actually modify environment variables:
1. Edit `.env.local` directly (not through Copilot)
2. For production, update Google Cloud Secret Manager
3. Update this tracker file to reflect changes (status/date only)

**Remember:** This app is production-ready and fully configured. Most requests from Darrell are about running/testing, not fixing configuration.
