# Environment Variables Configuration

# CORS Configuration
# Comma-separated list of allowed origins for the /api/ingest-strategy endpoint
# Example: https://app.example.com,https://dashboard.example.com
# Leave empty or unset to allow all origins (not recommended for production)
ALLOWED_ORIGINS=

# Firebase Configuration
# Get these from your Firebase project settings
# SECURITY NOTE: Firebase API keys with NEXT_PUBLIC_ prefix are intentionally
# client-side accessible. Security is enforced through Firebase Security Rules,
# not by hiding the API key. Ensure proper Firebase Security Rules are configured.
# See: https://firebase.google.com/docs/projects/api-keys
NEXT_PUBLIC_FIREBASE_API_KEY=
NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN=
NEXT_PUBLIC_FIREBASE_PROJECT_ID=
NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET=
NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID=
NEXT_PUBLIC_FIREBASE_APP_ID=

# Google AI API Key for Genkit
# SECURITY NOTE: This key should NEVER have the NEXT_PUBLIC_ prefix.
# It must remain server-side only. All AI operations use server actions
# to prevent exposing this key to the client.
GOOGLE_GENAI_API_KEY=
